/* 
    以前创建数组的方法是 new Array(10) 得到的是  =【empty X 10】,为了避免这种方式，es6引入了一下方式

    1.Array统一创造方式
        Array.of(10,20.30)

    2. 判断一个元素是不是一个纯数组 Array.isArray()

    3. Array.from()  将一个元素变成数组

    4. 实例方法  find 遍历数组，查询符合条件的第一个元素

    5.findIndex 遍历数组，查询找到的第一个值的下标
*/